export type IPractice = {
  documentId?: string;
  lastPresented: Date | null;
  tag: string;
  createdAt: Date;
  node: string;
  user: string;
  eFactor: number;
  tagId: string;
  nextDate: Date;
  lastCompleted: Date | null;
  q: number;
  updatedAt: Date;
  iInterval: number;
};
