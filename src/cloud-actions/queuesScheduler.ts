// import { db } from "./utils/admin";

export const queuesScheduler = () => {
  try {
    // const queuesDoc = db.collection
    // const jobs = db.collection("jobs")
  } catch (error) {
    console.log(error);
  }
};
